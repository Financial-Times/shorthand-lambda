machine:
  timezone: Europe/London
  node:
    version: 4.3.2

test:
  post:
    - bash <(curl -s https://codecov.io/bash)

deployment:
  lambda:
    branch: master
    commands:
      - make deploy

general:
  artifacts:
    - coverage
